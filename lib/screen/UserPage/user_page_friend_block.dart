import 'package:flutter/material.dart';

class UserPageFriendBlock extends StatelessWidget {
  const UserPageFriendBlock({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
